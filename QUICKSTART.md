# 🚀 Быстрый старт CMS-админки EvilBook

## Что уже сделано?

✅ **60% функционала готово!**

- Авторизация и защита маршрутов
- Редакторы для всех 5 секций сайта
- Загрузка изображений
- База данных с RLS политиками
- Панель управления

## Что нужно сделать для запуска?

### Шаг 1: Создать проект Supabase

1. Зайдите на https://supabase.com
2. Нажмите "New Project"
3. Заполните данные проекта и дождитесь создания

### Шаг 2: Выполнить миграции

1. В Supabase откройте **SQL Editor**
2. Скопируйте и выполните содержимое файла:
   ```
   supabase/migrations/001_initial_schema.sql
   ```
3. Затем выполните:
   ```
   supabase/migrations/002_seed_data.sql
   ```

### Шаг 3: Настроить Storage

1. Откройте раздел **Storage**
2. Нажмите "Create bucket"
3. Имя: `media`
4. ✅ Отметьте "Public bucket"
5. Нажмите "Create"

### Шаг 4: Создать администратора

1. Откройте **Authentication** → **Users**
2. Нажмите "Add user" → "Create new user"
3. Email: `admin@evilbook.com` (или любой другой)
4. Password: придумайте пароль
5. Нажмите "Create user"
6. **Скопируйте ID пользователя** (в списке пользователей)

7. Откройте **SQL Editor** и выполните:
   ```sql
   INSERT INTO public.user_roles (user_id, role)
   VALUES ('ВСТАВЬТЕ_СЮДА_ID_ПОЛЬЗОВАТЕЛЯ', 'admin');
   ```

### Шаг 5: Настроить переменные окружения

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   copy .env.example .env
   ```

2. Откройте `.env` и заполните:
   - В Supabase откройте **Settings** → **API**
   - Скопируйте **Project URL** → вставьте в `VITE_SUPABASE_URL`
   - Скопируйте **anon public** ключ → вставьте в `VITE_SUPABASE_ANON_KEY`

### Шаг 6: Запустить проект

```bash
npm run dev
```

### Шаг 7: Войти в админку

1. Откройте http://localhost:5173/admin/login
2. Введите email и пароль администратора
3. Готово! 🎉

## Что можно делать в админке?

### ✅ Уже работает:

1. **Главная** (`/admin`) - Статистика
2. **Секции сайта** (`/admin/sections`) - Редактирование контента:
   - Hero (главный баннер)
   - About (обо мне)
   - Subscribe (форма подписки)
   - Reviews Section (рецензии на главной)
   - Reviews Page (страница рецензий)

### ⏳ В разработке:

- Управление рецензиями
- Модерация комментариев
- Редактор навигации
- Галерея медиа
- Настройки сайта

## Как редактировать секцию?

1. Войдите в админку
2. Нажмите "Секции сайта"
3. Выберите нужную секцию (например, "Hero Section")
4. Измените поля
5. Загрузите изображение (если нужно)
6. Нажмите "Сохранить изменения"

## Проблемы?

### "Invalid API credentials"
→ Проверьте `.env` файл, правильно ли скопированы URL и ключ

### Не могу войти
→ Убедитесь, что роль 'admin' добавлена в таблицу user_roles

### Изображения не загружаются
→ Проверьте, что bucket 'media' создан и публичный

## Подробная документация

Смотрите файлы:
- `CMS_SETUP.md` - Полная инструкция
- `CMS_PROGRESS.md` - Что сделано и что осталось

---

**Удачи! 🚀**
